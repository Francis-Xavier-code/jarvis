# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
dist/
build/
uv.lock

# JARVIS data
data/
sessions/

# Local secrets / config — NEVER commit. Contains API keys.
config.toml
.env
*.local.toml

# Agent
.hermes/

# OS
.DS_Store

# Cloned plugins (pulled at runtime via bootstrap / jarvis install) must not be
# committed. Authoring plugins under plugins/ WITHOUT this marker stay tracked.
plugins/**/.jarvis-cloned
